#Usando o metodo da rejeição temos que o ponto q maximiza f(x)/g(x) onde g(x)=lambda*exp(-lambda*x)
#x=lambda. Pela teoria sabemos que E[X]=c, queremos minimar E[X], ou seja queremos achar 
#valor de c(lambda)=exp((lambda**2)/2)/(sqrt(2*pi)*lambda)  ==> lambda = +-1, como lmabda>0 temos que lambda=1
